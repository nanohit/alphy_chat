# Port (Render uses 10000)
PORT=10000

# Metered.ca TURN server (optional — falls back to STUN only)
# Sign up at https://www.metered.ca/ for free 500MB/month relay
# METERED_DOMAIN is your app name from the Metered dashboard (the "xyz" in xyz.metered.live)
# Both METERED_API_KEY and METERED_DOMAIN are required for TURN to work.
# Find your domain in the Metered dashboard — it's the app name (the "xyz" in xyz.metered.live).
METERED_API_KEY=
METERED_DOMAIN=
